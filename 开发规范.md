# 开发规范（AI 与人类开发者必读）

本规范是本仓库**唯一**的行为约束来源。目标：任何人在任何时间新增/修改功能，产出风格完全一致。

## 1. 总则

1. **强约定优先**：不确定怎么写时，照抄现有代码——标准业务模块抄 `src/modules/auth/`
   （6 文件分层 + 领域辅助文件），跨模块聚合抄 `src/modules/admin/`（门面模式），
   复杂业务（SSE/注册表/子进程/SDK 集成）抄 `src/modules/agent/`。它们就是标准。
2. **修改范围分级**：
   | 区域 | 权限 |
   |---|---|
   | `src/modules/**`、`src/db/schema.ts`、`core/error-codes.ts`（仅追加）、`src/env.ts`（追加变量） | ✅ 自由工作区 |
   | `ui/src/pages/**`、`ui/src/components/**`、`ui/src/hooks/**`、`ui/src/stores/**`、`ui/src/lib/**` | ✅ 自由工作区（规范见第 13 节） |
   | `src/core/**`（除 error-codes）、`src/app.ts`、`src/db/index.ts`、`src/index.ts`、`src/routes/index.ts` | ⛔ 默认禁止修改，确需改动先向用户说明理由 |
   | `ui/src/assets/*.css`（全局色彩）、`ui/vite.config.ts`、`ui/src/main.tsx`、`src/core/frontend.ts` | ⛔ 默认禁止修改，确需改动先向用户说明理由 |
   | `src/db/migrations/**`、`scripts/**` | ⛔ 禁止手改（migrations 只能由 drizzle-kit 生成；build 脚本改动视为构建链变更） |
3. **每次交付前自检**：`bun run typecheck && bun run lint && bun test` 三命令必须全绿；
   涉及前端再加 `cd ui && bun run build` 零错。
4. 依赖版本锁定见 `package.json` / `ui/package.json`，禁止擅自升级或新增依赖；确有必要先说明理由征得同意。

## 2. 技术栈（固定）

**后端**：bun + hono + `@hono/zod-openapi`（zod v4）+ drizzle-orm(postgres.js) + pino + biome + bun:test；
AI 能力经 `@anthropic-ai/claude-agent-sdk`（spawn CLI 子进程，streaming-input 常驻模式）；
API 文档 `@scalar/hono-api-reference`；辅助 dayjs / nanoid / qrcode（MFA 二维码）。

**前端**（`ui/`）：Vite + React 19 + TypeScript + Tailwind v4 + zustand + sonner + react-router-dom v7
+ react-markdown(remark-gfm)；基础组件 shadcn 范式（cva + cn，变体来源 base-ui 为主、radix 部分并存，见 13.1）；prettier 裁决格式。详见第 13 节。

## 3. 分层架构与各层禁令

```
route(*.route.ts)           解析请求 -> 调 service -> 包装响应
  └ service(*.service.ts)       业务逻辑、业务错误
      └ repository(*.repository.ts)   drizzle 查询
          └ db(src/db/index.ts)       连接与事务
```

| 层 | 必须 | 禁止 |
|---|---|---|
| route | handler 只做 `c.req.valid()` → service → `ok()/created()`；每个端点用 `createRoute` 声明请求/响应 schema | 写业务逻辑、写 try-catch、直接访问 db |
| service | `throw new AppError(...)` 表达业务错误；返回 DTO | import hono 任何内容；写 SQL |
| repository | 首参 `executor: DbExecutor`；返回原始 Row | 抛 AppError；做业务判断 |
| table | 只依赖 drizzle-orm | import 业务代码 |

**已知例外**（照抄，不要"修复"）：
- SSE 端点（`agent.route.ts` 的 `/session/events`）走 `app.get` + `streamSSE`，无 JSON 响应体，不硬套 createRoute；
- SDK 集成层文件（`session-registry.ts` 等）不属上述四层，见第 11 节的架构说明；
- `scripts/build-exe.ts` 允许 `console.log`（构建脚本输出面向终端，带 biome-ignore）。

## 4. 新增业务模块标准流程（唯一入口）

以新增 `product` 模块为例，共 8 步：

1. 新建 `src/modules/product/` 目录，6 个核心文件照抄 `src/modules/auth/` 改名
   （table / schema / repository / service / route / index；领域辅助文件按需追加，如 auth 的 jwt.ts）
2. 写 `product.table.ts`（pgTable 定义）
3. 在 `src/db/schema.ts` 登记：`export * from '@/modules/product/product.table'`
4. 写 `product.schema.ts`（请求/响应 DTO + toProduct 映射器）
5. 写 `product.repository.ts`
6. 写 `product.service.ts`；需要的错误码到 `core/error-codes.ts` **追加**（命名 `PRODUCT_XXX`）
7. 写 `product.route.ts` + `index.ts`（createApp + registerProductRoutes；
   需登录的模块在 index.ts 挂模块级 `app.use('*', requireAuth())`，路由不逐条声明 middleware）
8. `src/routes/index.ts` 挂载 `app.route('/api/products', productApp)`；
   然后 `bun run db:generate && bun run db:migrate`；补 `product.test.ts`

## 5. 各层代码样板（可直接拷贝改名）

### 5.1 table 层（`*.table.ts`）

```ts
import { index, pgEnum, pgTable, text, timestamp, uuid } from 'drizzle-orm/pg-core'

export const productStatus = pgEnum('product_status', ['on_sale', 'off_sale'])

export const products = pgTable(
  'products',
  {
    id: uuid().primaryKey().defaultRandom(),
    name: text().notNull(),
    price: integer().notNull(),                     // 按需选择列类型
    status: productStatus().notNull().default('on_sale'),
    createdAt: timestamp({ withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp({ withTimezone: true }).notNull().defaultNow().$onUpdate(() => new Date()),
  },
  (t) => [index('products_status_idx').on(t.status)],
)

export type ProductRow = typeof products.$inferSelect
```

要点：列名不写字符串（全局 `casing: 'snake_case'` 自动转换）；索引用数组返回风格；
时间列一律 `defaultNow()` + `$onUpdate`，业务代码**禁止** `new Date()` 赋值。
表写在模块内 `*.table.ts`，由 `src/db/schema.ts` 整体 re-export 即完成登记。

### 5.2 schema 层（`*.schema.ts`）

```ts
import { z } from '@hono/zod-openapi'   // ★ 唯一合法的 z 导入源
import type { ProductRow } from './product.table'

export const ProductIdParam = z.object({
  id: z.string().uuid().openapi({ param: { name: 'id', in: 'path' } }),
})

export const CreateProductInput = z.object({
  name: z.string().min(1).max(200).openapi({ example: '商品名' }),
  price: z.number().int().min(0),
})

// ★ 更新输入必须显式 optional，禁止 CreateInput.partial() 派生（default 会清空未提交字段）
export const UpdateProductInput = z.object({
  name: z.string().min(1).max(200).optional(),
  price: z.number().int().min(0).optional(),
})

export const ProductDto = z
  .object({
    id: z.string().uuid(),
    name: z.string(),
    price: z.number().int(),
    status: z.enum(['on_sale', 'off_sale']),
    createdAt: z.iso.datetime(),     // zod v4 语法，禁用已弃用的 z.string().datetime()
    updatedAt: z.iso.datetime(),
  })
  .openapi('Product')

export type Product = z.infer<typeof ProductDto>
export type CreateProductData = z.infer<typeof CreateProductInput>

export const toProduct = (row: ProductRow): Product => ({
  ...row,
  createdAt: row.createdAt.toISOString(),
  updatedAt: row.updatedAt.toISOString(),
})
```

### 5.3 repository 层（`*.repository.ts`）

```ts
import { desc, eq, sql } from 'drizzle-orm'
import type { DbExecutor } from '@/db'
import { getOffset, type PageQuery } from '@/utils/pagination'
import { products, type ProductRow } from './product.table'
import type { CreateProductData } from './product.schema'

export const productRepository = {
  async list(executor: DbExecutor, filter: PageQuery & { status?: 'on_sale' | 'off_sale' }) {
    const where = filter.status ? eq(products.status, filter.status) : undefined
    const rows = await executor
      .select()
      .from(products)
      .where(where)
      .orderBy(desc(products.createdAt))
      .limit(filter.pageSize)
      .offset(getOffset(filter))
    const [countRow] = await executor
      .select({ count: sql<number>`count(*)::int` })
      .from(products)
      .where(where)
    return { rows, total: countRow?.count ?? 0 }
  },
  async findById(executor: DbExecutor, id: string): Promise<ProductRow | undefined> {
    const [row] = await executor.select().from(products).where(eq(products.id, id)).limit(1)
    return row
  },
  async create(executor: DbExecutor, data: CreateProductData): Promise<ProductRow> {
    const [row] = await executor.insert(products).values(data).returning()
    return row!
  },
  // update / remove 同 auth 模式
}
```

### 5.4 service 层（`*.service.ts`）

```ts
import { AppError } from '@/core/app-error'
import { log } from '@/core/logger'
import { db } from '@/db'
import { productRepository } from './product.repository'
import { toProduct, type CreateProductData } from './product.schema'

export const productService = {
  async getById(id: string) {
    const row = await productRepository.findById(db, id)
    if (!row) {
      throw new AppError('PRODUCT_NOT_FOUND')          // 业务错误唯一表达方式
    }
    return toProduct(row)
  },
  async create(data: CreateProductData) {
    const row = await productRepository.create(db, data)
    log().info({ productId: row.id }, '商品已创建')      // 请求内自动携带 traceId + username
    return toProduct(row)
  },
  // 跨表事务范式：
  // await db.transaction(async (tx) => {
  //   await productRepository.create(tx, data)
  //   await stockRepository.decrease(tx, ...)
  // })
}
```

### 5.5 route 层（`*.route.ts`）

```ts
import { createRoute, z } from '@hono/zod-openapi'
import {
  ApiResponseSchema, created, ErrorResponseSchema, jsonResponse, ok, PaginatedSchema,
} from '@/core/response'
import type { App } from '@/core/types'
import { PageQuerySchema } from '@/utils/pagination'
import { ProductDto, ProductIdParam, CreateProductInput } from './product.schema'
import { productService } from './product.service'

const createProductRoute = createRoute({
  method: 'post',
  path: '/',
  tags: ['product'],
  summary: '创建商品',
  request: {
    body: { required: true, content: { 'application/json': { schema: CreateProductInput } } },
  },
  responses: {
    201: jsonResponse(ApiResponseSchema(ProductDto), '已创建'),
    422: jsonResponse(ErrorResponseSchema, '参数错误'),
  },
})

export function registerProductRoutes(app: App): void {
  app.openapi(createProductRoute, async (c) => {
    return created(c, await productService.create(c.req.valid('json')))
  })
}
```

要点：列表接口 query 基于 `PageQuerySchema.extend({...})`；
路径参数用 OpenAPI 风格 `/{id}`；`body` 必须带 `required: true`；
每个端点至少声明 2xx 与 422/404 响应。

`index.ts`（模块出口）：

```ts
import { createApp } from '@/core/create-app'
import { registerProductRoutes } from './product.route'

const app = createApp()          // ★ 禁止 new OpenAPIHono()
registerProductRoutes(app)
export const productApp = app
```

## 6. 横切关注点使用规范

### 6.1 认证与用户身份（已实现，勿重复造）

- 身份唯一来源：HttpOnly cookie `token` 中的 JWT（`core/middleware/auth.ts` 验签解出 username），
  **绝不信任**客户端 header 声明的用户名
- `requireAuth()` 在模块 `index.ts` 挂载（`app.use('*', requireAuth())`）；handler 里
  `c.get('username')` 取当前用户
- requireAuth 副作用：把 username 注入该请求的 ALS logger，请求内所有 `log()` 自动携带
- 登录链路（账号密码 / SOAP 外部认证 / MFA-TOTP / 失败锁定 / secret 加密存储）全在 auth 模块，
  与业务模块无关，不要在业务代码里再做身份判断

### 6.2 日志
- 请求内：`log().info({ 字段 }, '消息')` —— 自动携带 traceId/username；启动/后台任务/子进程：
  `getLogger('名字')` 显式传递（脱离请求 ALS 的场景，如 session-registry 内的会话级 logger）
- 结构化字段一律**蛇形命名**（orderId、sessionId），消息用中文短句
- ⛔ 禁止 `console.*`（唯一例外 env.ts 启动失败与 build 脚本，须带 biome-ignore 注释）
- ⛔ 禁止把敏感信息（密码/token/密钥）写入日志（pino redact 是兜底不是借口）

### 6.3 错误
- 业务错误唯一方式：`throw new AppError('CODE')`，可选 `{ message, details }`
- 新错误码只在 `core/error-codes.ts` **追加**：`模块_原因` 大写下划线 + 默认中文消息
- ⛔ 禁止裸 `throw new Error()`；禁止 route 层 try-catch（错误统一走 onError）

### 6.4 校验（zod v4 语法注意）
- query 参数是字符串：数字一律 `z.coerce.number()`
- ISO 时间：`z.iso.datetime()`（`z.string().datetime()` 已弃用）
- 错误树：`z.treeifyError(err)`
- `.default()` 只用于 Create 输入；Update 输入显式 `.optional()`

### 6.5 traceId
- 自动携带（请求头透传/生成、日志、错误体），**禁止**手工传递或修改

## 7. 数据库规范

1. 改表：改 `*.table.ts` → `bun run db:generate`（生成迁移）→ `bun run db:migrate`（执行）
2. ⛔ 禁止 `drizzle-kit push`（绕过迁移历史）；禁止手改 `migrations/` 任何文件
3. 迁移命令必须用 `bun run db:generate/migrate`（内部 bunx，自动读 .env）
4. 编译版部署：启动即迁移（`MIGRATE_ON_START=true MIGRATIONS_DIR=./migrations`，默认已开）
5. 本地库：`bun run docker:db` 起共享 postgres 容器，各项目独立 database

## 8. 测试规范

- 框架 `bun:test`，文件 `*.test.ts` 放模块目录内
- 集成测试用 `app.request()`（`import { app } from '@/app'`，无需起端口）
- **每个请求必须带 `headers: { 'content-type': 'application/json' }`**，否则 body 静默为空
- 模式：模块顶层 try 连接 DB，`describe.skipIf(!dbReady)` 无库跳过
  （照抄 `src/modules/auth/auth.api.test.ts` 开头；纯逻辑单测见 `src/modules/agent/agent.test.ts`；
  persona CRUD 集成测试见 `agent.persona.test.ts`）
- 新端点必须覆盖：成功路径 + 主要错误路径（404/422/409）
- 依赖真实 CLI 子进程的链路（会话开启/SSE/切换）不写自动化，走手工验证

## 9. 命名与风格

- 文件 kebab-case + ASCII（如 `product.repository.ts`）；禁止中文/拼音文件名
- 变量 camelCase；类型 PascalCase；常量 PascalCase（Users、ErrorCodes）
- 格式由 biome 裁决：单引号、无分号、2 空格缩进、行宽 100（提交前 `bun run lint:fix`）

## 10. 禁止事项清单（Do-NOT，后端；前端见 13.6）

| # | 禁止 |
|---|---|
| 1 | `new OpenAPIHono()` / `new Hono()` —— 必须用 `createApp()` |
| 2 | 从 `'zod'` 导入 z —— 必须从 `'@hono/zod-openapi' 导入` |
| 3 | 修改 `src/core/**`（`error-codes.ts` 追加除外） |
| 4 | route handler 内写业务逻辑 / try-catch / 直接访问 db |
| 5 | repository 层抛 AppError / 做业务判断 |
| 6 | 手写 DDL、手改 `migrations/`、使用 `db:push` |
| 7 | `console.*`、裸 `throw new Error()` |
| 8 | 业务代码直接读 `Bun.env` / `process.env`（一律 `import { env } from '@/env'`） |
| 9 | 时间列用 `new Date()` 赋值（用 `defaultNow()` / `$onUpdate`） |
| 10 | 引入新依赖、升级依赖版本而不说明理由 |
| 11 | Update 输入用 Create 输入的 `.partial()` 派生（default 陷阱） |
| 12 | 运行 `docker compose down -v`、删除数据库等破坏性命令 |
| 13 | 在 handler 里读 header 声明的用户名当身份（一律 `c.get('username')`） |

## 11. 模块地图与架构要点

三个业务模块，`src/routes/index.ts` 是路由总表（挂载点一目了然）：

### 11.1 auth（`/api/auth`）——标准范式 + 登录领域

6 核心文件 + 领域辅助：`jwt.ts`（签发/验签）、`totp.ts`（MFA 动态码）、`soap.ts`（外部
SOAP 认证源）、`secret-cipher.ts`（secret 加密存储）。登录失败锁定、MFA 绑定/校验、
users 表含 role 字段（'user'/'admin'，admin 判定用白名单提升，二期做接口级权限）。

### 11.2 admin（`/api/admin`）——门面模块

不含自己的表，聚合多模块数据出管理端接口（用户列表 / 重置 MFA / 运行看板 stats——
stats 跨查 agent 的内存注册表与 DB）。模块级 requireAuth。前端对应 `ui/src/pages/admin/`。

### 11.3 agent（`/api/agent`）——核心复杂模块（Web 版 Claude Code）

除 6 核心文件外，多出一层 **SDK 集成层**（不遵循四层禁令，按子进程管理范式写）：

| 文件 | 职责 |
|---|---|
| `session-registry.ts` | 内存注册表（Map<dir, ctx>）：CLI 子进程生命周期、dir 锁、evict 驱逐、状态机（starting→turn-running→idle）、空闲 GC、绝对寿命、每用户配额 |
| `sdk-input-stream.ts` | CLI stdin 流式输入封装 |
| `agent-query-options.ts` | SDK query() options 构造（env 注入/审批回调/persona append 注入）；铁律：绝不传 allowedTools 裸内置工具名 |
| `approval-manager.ts` | canUseTool 审批挂起/放行 |
| `sse-events.ts` | 会话事件类型与 seq 协议 |
| `agent-event-translator.ts` | SDK 原始事件 → 前端展示事件翻译 |
| `agent-session-history.ts` | 会话 JSONL 历史重放为消息列表 |
| `cli-path.ts` / `cli-path.win32.ts` | CLI 可执行文件解析（编译版解包路径） |
| `user-config.ts` | 多用户隔离：per-user `CLAUDE_CONFIG_DIR` |
| `paths.ts` | 目录 base64url 编解码（Windows 路径 HTTP 安全传输） |

**会话协议**：POST/DELETE 类带 header `x-session-id` + `x-workspace-dir`(base64url)；
SSE 端点 `/session/events` 走 query 参数（EventSource 不支持自定义 header）。
身份一律取 JWT 解出的 username（多用户隔离入口：注册表按 dir 键做属主校验）。

**persona（智能体）**：定义存 `agent_personas` 表；会话绑定用快照表 `agent_session_personas`
（personaName/systemPrompt 落库，persona 事后增删改不影响已开会话）；注入方式为
`systemPrompt: { preset: 'claude_code', append }`；空闲切换 = evict 旧进程 + 同 sid resume。

**改 agent 模块注意**：dev 热重载（`bun run --hot`）会中断活跃 CLI 子进程导致会话
process_exit——属预期现象，刷新页面走恢复链路即可；排查此类问题看 `logs/` 下当日日志。

## 12. 构建与部署

| 命令 | 说明 |
|---|---|
| `bun run dev` | 后端热重载（http://localhost:3000，文档 /docs）；前端 `cd ui && bun run dev`（5173，proxy 转 3000） |
| `bun run build` | 打包单文件可执行到 `bin/`（内嵌前端页面与迁移，命名 app-{platform}-{arch}-{version}[.exe]）；`-- --target=bun-linux-x64` 交叉编译 |
| `bun run docker:db` | 起本地 postgres 容器 |

- 编译版运行：`cd bin && DATABASE_URL=... PORT=... ./app-...`（bin 下放 .env 亦可）；启动即迁移
- **子路径部署（BASE_URL）**：根 `.env` 设 `BASE_URL=/claude`（必须 `/` 开头、无协议主机、无尾斜杠）；
  构建时 `BASE_URL=... bun run build` 驱动 vite base，**启动 exe 时须设相同值**；
  开发期 ui 的 vite.config 同读此变量剥前缀转发。前端代码不做环境分支（见 13.5）

## 13. 前端（ui/）开发规范

### 13.1 总则与技术栈（固定）

1. **强约定优先**：不确定怎么写时，照抄现有代码——
   - 业务页面：管理页抄 `ui/src/pages/admin/`（AdminLayout + 子页面 + MENU 登记），复杂交互页抄 `ui/src/pages/ChatPage.tsx`
   - store 抄 `ui/src/stores/auth.ts`；AI 会话状态机抄 `ui/src/hooks/useChatAgent.ts`
   - 基础组件抄 `ui/src/components/ui/button.tsx`（cva variant 模式）
2. 技术栈锁定（版本见 `ui/package.json`）：

   | 库 | 用途 |
   |---|---|
   | Vite + React 19 + TypeScript | 工程底座（`@` alias → `ui/src/`） |
   | Tailwind CSS v4 | 样式（`@tailwindcss/vite` 插件，无 tailwind.config，token 在 main.css `@theme inline`） |
   | shadcn 范式组件（cva + clsx + tailwind-merge） | `components/ui/` 基础组件；变体来源混合：**base-ui 为主**（button/dialog/checkbox/radio-group…），radix 并存 5 个（avatar/dropdown-menu/label/separator/tooltip）——新组件用 shadcn CLI 拉取（components.json 已配 alias），拉什么变体照单全收不手工改造 |
   | lucide-react | 图标（具名导入 tree-shaking） |
   | sonner | toast（统一从 `@/components/ui/sonner` 导入 `toast`） |
   | react-router-dom v7 | 路由（Route 只在 `App.tsx` 登记） |
   | zustand | 状态管理（`stores/` 一域一文件） |
   | react-markdown + remark-gfm | 消息渲染 |
   | prettier | 格式化裁决（singleQuote、semi: false、printWidth 100） |

3. **工具链边界**：biome/tsc/bun:test 只管后端（`biome.json` 已排除 `ui/`）；前端自检 =
   `cd ui && bun run build`（tsc -b + vite build）零错；格式化 `cd ui && bun run format`。
4. ⛔ 引入新依赖（尤其 UI 库 / 状态库 / 请求库）必须先说明理由征得同意。

### 13.2 目录结构与职责

```
ui/src/
├── App.tsx               路由表（Routes/Route 唯一登记处；含 AuthGuard 包裹）
├── main.tsx              入口（BrowserRouter + 全局 <Toaster />）
├── assets/               base.css（暖灰阶梯变量）+ main.css（HSL 语义变量）——全局色彩，⛔ 谨慎区
├── components/
│   ├── ui/               基础组件（button/card/dialog/sonner…，cva + cn 模式）
│   ├── chat/             聊天域组件（消息列表/输入框/审批卡片/工具结果渲染）
│   │   ├── chat-input/   输入框（PromptInput + 命令/@提及/智能体选择弹层）
│   │   ├── PermissionRequest/   工具审批卡片（按工具类型分文件）
│   │   └── tool-results/        工具结果特化渲染（Bash/Read/Grep/Json…）
│   ├── AuthGuard.tsx     登录守卫
│   └── CaptchaPuzzle.tsx 登录验证码
├── hooks/
│   ├── useChatAgent.ts        核心：SSE 连接/会话状态机/代际锁（gen）防旧操作回写
│   ├── useChatAgentApi.ts     desktop ChatAgentApi 同形适配层（见 13.4）
│   └── useIsMobile.ts
├── lib/
│   ├── api.ts            类型化 API 客户端（解包 { data }，非 2xx 抛 ApiError；withBase 处理子路径）
│   ├── agent-api.ts      agent 域 REST 封装（含 header 协议/SSE URL 构造）
│   ├── agent-types.ts    agent 域 DTO（对齐后端 agent.schema.ts）
│   ├── sse-client.ts     SSE 客户端（断线重连/seq 续传）
│   ├── admin-api.ts      管理端 API 封装
│   ├── format.ts / xml-tags.ts / utils.ts
├── pages/
│   ├── ChatPage.tsx      聊天主页面（三栏布局）
│   ├── LoginPage.tsx
│   └── admin/            管理区（AdminLayout + MENU + Dashboard/Personas/Settings 子页面）
└── stores/               zustand（auth / chat / compact-request，一域一文件）
```

### 13.3 新增页面标准流程（唯一入口）

以新增管理员子页面「产品管理」为例，共 5 步：

1. 需要跨组件共享的数据先写 `stores/product.ts`（照抄 `stores/auth.ts`：state 字段 + async action，
   action 内走 `api` 客户端，返回 boolean 供调用方 toast）
2. 新建 `pages/admin/ProductPage.tsx`：函数组件，数据拉取放 `useEffect(() => { void fetch() }, [])`，
   操作反馈用 `toast.success / toast.error`
3. `pages/admin/AdminLayout.tsx` 的 MENU 登记入口；`App.tsx` 登记 Route（admin 路由统一挂 AuthGuard）
4. 若后端是新接口：DTO 手写 TS interface 对齐后端 `*.schema.ts` 的 Dto（字段名/可空性一致），
   放 `lib/agent-types.ts` 或页面内定义并导出，供 `api.get<Product[]>` 泛型使用
5. 自检：`cd ui && bun run build` 零错

### 13.4 AI 会话前端分层（chat 域专用，勿打散）

```
agent-types.ts（DTO） → agent-api.ts（REST） → useChatAgent.ts（SSE + 状态机）
                                          → useChatAgentApi.ts（组件消费接口）
                                               → components/chat/**（纯展示组件）
stores/chat.ts（消息视图状态，useChatAgent 写入）
```

- `useChatAgentApi` 是 **desktop ChatAgentApi 同形适配层**：ported 组件
  （ChatMessageList/PromptInput/CostCircle…）只消费这个接口，除类型导入路径外零改动——
  改动会话逻辑时保持接口形状稳定
- 并发防护：`useChatAgent` 用代际锁（`genRef` 自增）丢弃过期异步结果；SSE 重连由
  `sse-client` 处理，业务层不手动 EventSource
- 会话被服务端回收（404）有自动恢复链路，UI 层只处理 `closed` 终态

### 13.5 横切约定

- **API 唯一入口**是 `lib/api.ts` 的 `api` 对象（自动解包 `data`、统一 Content-Type；
  子路径部署自动 `withBase`）；后端契约：成功 `{ data }` / 失败 `{ error: { code, message, traceId } }`
- **样式只用语义 token**：`bg-background`、`text-muted-foreground`、`border` 等
  （变量定义在 main.css，含 `.dark`）；⛔ 业务代码禁止硬编码色值（#xxx / rgb）
- **开发/生产同源**：请求一律相对路径（`/api/...`）——开发经 vite proxy 转 3000，
  生产由可执行文件内嵌服务；前端代码**不做环境分支**（BASE_URL 差异由构建/部署层吸收）
- **console 允许**（前端无 pino，devtools 场景）：仅用于开发排查日志；
  面向用户的错误提示一律 toast，禁止 alert/confirm
- 基础组件追加到 `components/ui/` 用 cva variant 模式；聊天域业务组件跟随 `components/chat/` 就近分目录
- 图标一律 lucide-react 具名导入（tree-shaking）

### 13.6 前端禁止事项清单（Do-NOT）

| # | 禁止 |
|---|---|
| 1 | 绕过 `lib/api.ts` 直接 fetch / 引入 axios 等请求库 |
| 2 | 引入新 npm 依赖（UI / 状态 / 请求 / 样式库）而不先征得同意。shadcn CLI 拉组件允许（components.json 已锁配置，源码拷入 components/ui）；但 CLI 要安装新 npm 包时仍需先征得同意 |
| 3 | 业务代码硬编码色值、魔法 px 主题值（用语义 token） |
| 4 | 修改 `assets/*.css` 全局色彩变量、`vite.config.ts`、`main.tsx` 而不说明理由 |
| 5 | 在 `App.tsx` 堆业务逻辑（页面独立文件，App 只留路由表） |
| 6 | 路由路径硬编码散落各处（集中 App.tsx；跳转用 `<Link>` / `navigate`） |
| 7 | 深层相对路径导入（一律 `@/`） |
| 8 | 在 ui 内混用后端代码风格（分号、双引号——prettier 裁决：无分号单引号） |
| 9 | 手动 new EventSource / 手写重连（用 `lib/sse-client.ts`） |
| 10 | 绕过 `useChatAgentApi` 直接从 `useChatAgent` 取状态喂 ported 组件（接口形状是为组件冻结的契约） |
