import { Markdown } from './Markdown'

const GUIDE_MD = `## Claude Code · Web 版使用指南

> 浏览器里的编程智能体：读代码、改文件、跑命令、修 bug，全程征求你的同意

### 开始

左侧选择项目（工作区）→ 新建或继续一个会话 → 直接描述需求。AI 在项目目录内工作，改动只落在服务器上的工作区文件。

### 输入框

- **Enter 发送** / **Shift+Enter 换行**；手机上 Enter 是换行，点 **发送按钮** 发送
- 打 **@** 引用文件（模糊搜索）、**/** 用斜杠命令、**⚡快捷** 一键填入常用任务模板
- 支持拖拽/粘贴图片，拖项目内文件进输入框可直接引用

### 安全与控制

- AI 改文件、执行命令前会**弹卡片征求你同意**，可编辑参数、选「总是允许」
- AI 跑偏了按 **Esc** 立即停下（**Shift+Esc** 关闭会话），会话不丢，随时继续
- 对文件改动不满意：点自己那条消息旁的**回滚**，文件恢复到发消息之前
- 顶部**圆环**是上下文用量：接近上限会自动压缩历史对话，也可点开手动压缩

### 多端与多人

- 手机浏览器可用；窄屏下侧栏收为抽屉，功能不缩水
- 同一会话同时只允许一人使用：被别处占用时会提示，确认后可接管（对方断开）
- 历史会话随时接着聊；想换模型在输入框左下角选（下一次会话生效）`

export function UsageGuide(): React.JSX.Element {
  return (
    <div className="h-full overflow-auto">
      <div className="mx-auto max-w-3xl px-6 py-8">
        <Markdown>{GUIDE_MD}</Markdown>
      </div>
    </div>
  )
}
